/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com', "i.imgur.com"], // Add 'fakestoreapi.com' to the list of allowed image domains
      },
};

export default nextConfig;
